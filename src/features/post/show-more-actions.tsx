import { IconButton } from "shared/ui";

import { MoreVertIcon } from "shared/icons"

export const ShowMoreActions = () => (
    <IconButton aria-label="settings">
        <MoreVertIcon />
    </IconButton>
)